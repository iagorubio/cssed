cssed (c) Iago Rubio, 2003


To build this package run the commands:

./configure
make

To install it:

make --install (as root)

If you have updated this package from CVS run:

./bootstrap 

or./autogen.sh

to rebuild the scripts if you run in problems with previous build scripts.

 
